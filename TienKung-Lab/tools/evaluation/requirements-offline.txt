PyYAML>=6
numpy>=1.24
# Integration dispatches to trapezoid when available, otherwise trapz (NumPy 1.x).
matplotlib>=3.6
pytest>=7
# CPU checkpoint auditing additionally uses the training environment's torch.
# DOCX output uses Python stdlib OOXML. LibreOffice is optional for visual QA.
