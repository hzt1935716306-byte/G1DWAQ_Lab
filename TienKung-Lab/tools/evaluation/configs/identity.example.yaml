# Copy to logs/training_run/evaluation_identity.yaml and fill with verified evidence.
# Paths below are relative to that manifest, not relative to this example file.
checkpoint_sha256: REPLACE_WITH_CHECKPOINT_SHA256
agent_config_sha256: REPLACE_WITH_ORIGINAL_AGENT_YAML_SHA256
env_config_sha256: REPLACE_WITH_ORIGINAL_ENV_YAML_SHA256
task_name: g1_plane_v1_estimator_context_no_reward_matched
# Set true only after explicitly confirming legacy task identity when evidence is insufficient.
task_confirmed: false
# Unique ID shared ONLY by checkpoints from the same training invocation.
# Leave unknown metadata null; do not infer transitions from the filename.
training_run_id: null
training_transitions: null
resources:
  native_nominal:
    path: ../../tools/recovery/generated/g1_plane_nominal_params_g1_slope_sys_d_candidate.yaml
    sha256: REPLACE_WITH_VERIFIED_NATIVE_NOMINAL_SHA256
  native_capability:
    path: ../../tools/recovery/generated/g1_recovery_params.yaml
    sha256: REPLACE_WITH_VERIFIED_CAPABILITY_SHA256
